name: Comprehensive Test Suite

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main ]

permissions:
  contents: read

jobs:
  frontend-tests:
    runs-on: ubuntu-latest
    env:
      NPM_CONFIG_ENGINE_STRICT: "false"
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json
    
    - name: Install frontend dependencies
      run: |
        cd frontend
        npm ci
    
    - name: Run frontend unit tests
      run: |
        cd frontend
        npm run test:ci
    
    - name: Run frontend performance tests
      run: |
        cd frontend
        npm run test:performance -- --run
    
    - name: Run visual regression tests
      run: |
        cd frontend
        npm run test:visual -- --run
    
    - name: Upload coverage reports
      uses: codecov/codecov-action@v3
      with:
        directory: frontend/coverage

  backend-tests:
    runs-on: ubuntu-latest
    env:
      DB_HOST: 127.0.0.1
      DB_PORT: 3306
      DB_NAME: asteroid_spectral_test
      DB_USER: root
      DB_PASSWORD: testpassword
    
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: testpassword
          MYSQL_DATABASE: asteroid_spectral_test
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install backend dependencies
      run: |
        cd backend
        pip install -r requirements.txt
        pip install -r requirements-dev.txt
    
    - name: Setup test database
      run: |
        mysql -h 127.0.0.1 -u root -ptestpassword -e "CREATE DATABASE IF NOT EXISTS asteroid_spectral_test;"
    
    - name: Run backend unit tests
      run: |
        cd backend
        python -m pytest tests/ -v --cov=app --cov-report=xml
    
    - name: Run integration tests
      run: |
        cd backend
        if [ -f test_api_integration.py ] || [ -f test_spectral_integration.py ]; then
          python -m pytest test_api_integration.py test_spectral_integration.py -v
        else
          echo "No legacy integration test files found, skipping this step."
        fi
    
    - name: Upload coverage reports
      uses: codecov/codecov-action@v3
      with:
        directory: backend

  e2e-tests:
    runs-on: ubuntu-latest
    needs: [frontend-tests, backend-tests]
    env:
      DB_HOST: 127.0.0.1
      DB_PORT: 3306
      DB_NAME: asteroid_spectral_test
      DB_USER: root
      DB_PASSWORD: testpassword
      FLASK_ENV: testing
      NPM_CONFIG_ENGINE_STRICT: "false"
    
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: testpassword
          MYSQL_DATABASE: asteroid_spectral_test
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        cd frontend && npm ci
        cd ../backend && pip install -r requirements.txt
    
    - name: Setup test database
      run: |
        mysql -h 127.0.0.1 -u root -ptestpassword -e "CREATE DATABASE IF NOT EXISTS asteroid_spectral_test;"
    
    - name: Start backend service
      run: |
        cd backend
        python app.py &
        sleep 10
    
    - name: Start frontend service
      run: |
        cd frontend
        npm run build
        npm run preview &
        sleep 10
    
    - name: Run Cypress E2E tests
      uses: cypress-io/github-action@v6
      with:
        working-directory: frontend
        wait-on: 'http://localhost:3000, http://localhost:5000/api/classifications'
        wait-on-timeout: 120
        browser: chrome
        headless: true
    
    - name: Upload Cypress screenshots
      uses: actions/upload-artifact@v3
      if: failure()
      with:
        name: cypress-screenshots
        path: frontend/cypress/screenshots
    
    - name: Upload Cypress videos
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: cypress-videos
        path: frontend/cypress/videos

  load-tests:
    runs-on: ubuntu-latest
    needs: [e2e-tests]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main')
    env:
      DB_HOST: 127.0.0.1
      DB_PORT: 3306
      DB_NAME: asteroid_spectral_test
      DB_USER: root
      DB_PASSWORD: testpassword
      FLASK_ENV: testing
    
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: testpassword
          MYSQL_DATABASE: asteroid_spectral_test
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install backend dependencies
      run: |
        cd backend
        pip install -r requirements.txt
        pip install -r requirements-dev.txt
    
    - name: Setup test database
      run: |
        mysql -h 127.0.0.1 -u root -ptestpassword -e "CREATE DATABASE IF NOT EXISTS asteroid_spectral_test;"
    
    - name: Start backend service
      run: |
        cd backend
        python app.py &
        sleep 10
    
    - name: Run load tests
      run: |
        cd backend
        python -m pytest tests/test_load_performance.py -v -s
    
    - name: Run Locust load tests
      run: |
        cd backend
        locust -f locustfile.py --host=http://localhost:5000 --headless -u 20 -r 4 -t 60s --html=load_test_report.html
    
    - name: Upload load test report
      uses: actions/upload-artifact@v3
      with:
        name: load-test-report
        path: backend/load_test_report.html

  security-tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'
    
    - name: Upload Trivy scan results to GitHub Security tab
      if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false)
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: 'trivy-results.sarif'

  test-report:
    runs-on: ubuntu-latest
    needs: [frontend-tests, backend-tests, e2e-tests, load-tests]
    if: always()
    
    steps:
    - name: Generate test report
      run: |
        echo "# Test Suite Results" > test_report.md
        echo "" >> test_report.md
        echo "## Summary" >> test_report.md
        echo "- Frontend Tests: ${{ needs.frontend-tests.result }}" >> test_report.md
        echo "- Backend Tests: ${{ needs.backend-tests.result }}" >> test_report.md
        echo "- E2E Tests: ${{ needs.e2e-tests.result }}" >> test_report.md
        echo "- Load Tests: ${{ needs.load-tests.result }}" >> test_report.md
        echo "" >> test_report.md
        echo "Generated: $(date)" >> test_report.md
    
    - name: Upload test report
      uses: actions/upload-artifact@v3
      with:
        name: test-report
        path: test_report.md
